name: enat_ai
description: Local LLM app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  llama_cpp_dart: ^0.0.9
  file_picker: ^8.0.0
  shared_preferences: ^2.3.0
  path_provider: ^2.1.2
  path: ^1.9.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
